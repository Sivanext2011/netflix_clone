apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: netflixclone-default
  namespace: devsecops
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: netflixclone
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - protocol: TCP
          port: 5000
  egress:
    - to:
        - namespaceSelector: {}
