apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: netflixclone
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: netflixclone.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: netflixclone
                port:
                  number: 80
